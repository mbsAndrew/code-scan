import React from 'react';

const Results = () => {
    return (
        <>
            <h2 className={"card-title"}>Results</h2>            
        </>
    );
}

export default React.memo(Results);
